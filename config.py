# connection parameters
user = "postgres"
password = "password"
host = "localhost"
database = "jobs_db"
param_dic = {
    "host" : host,
    "database" : database,
    "user" : user,
    "password" : password
}